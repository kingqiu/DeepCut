# DeepCut 前端交互完整测试计划

## 测试环境要求
- 服务：Next.js (localhost:3001)、Engine API (localhost:8080)、Redis、PostgreSQL
- 浏览器：Chrome/Chromium (Playwright)
- 测试数据：至少 1 个测试视频文件

---

## 测试模块划分

### 模块 1：首页 - 导航与布局 (HomePage Navigation)
**测试场景：**
1. 页面加载与基础元素
   - [ ] 页面成功加载，显示 "DeepCut" 标题
   - [ ] Header 显示 Logo、标题、副标题
   - [ ] Settings 图标可见且可点击
   - [ ] 显示 "新建切片任务" 标题

2. 导航功能
   - [ ] 点击 Settings 图标跳转到 `/settings`
   - [ ] 从设置页点击返回按钮回到首页

3. Tab 切换
   - [ ] 默认显示 "按项目" Tab
   - [ ] 点击 "全局片段库" Tab 切换成功
   - [ ] Tab 切换时内容正确更新

---

### 模块 2：视频上传 - 文件上传 (File Upload)
**测试场景：**
1. 文件选择上传
   - [ ] 点击 "选择文件" 按钮打开文件选择器
   - [ ] 选择有效视频文件（MP4）后显示上传进度
   - [ ] 上传进度条从 0% 增长到 100%
   - [ ] 上传成功后显示成功提示消息
   - [ ] 项目列表自动刷新，显示新项目卡片

2. 拖拽上传
   - [ ] 拖拽视频文件到上传区域，边框高亮
   - [ ] 释放文件后开始上传
   - [ ] 上传成功后项目列表更新

3. 错误处理
   - [ ] 上传非视频文件（如 .txt）显示错误提示
   - [ ] 上传超过 500MB 的文件显示错误提示
   - [ ] 网络错误时显示错误提示

---

### 模块 3：视频上传 - 本地路径提交 (Local Path)
**测试场景：**
1. 路径输入与提交
   - [ ] 切换到 "本地路径" Tab
   - [ ] 输入有效的本地视频路径
   - [ ] 点击 "提交" 按钮提交成功
   - [ ] 显示成功提示消息
   - [ ] 项目列表自动刷新

2. 键盘交互
   - [ ] 输入路径后按 Enter 键提交
   - [ ] 提交成功后输入框清空

3. 错误处理
   - [ ] 提交空路径时按钮保持禁用
   - [ ] 提交无效路径显示错误提示

---

### 模块 4：项目列表 (Project List)
**测试场景：**
1. 项目卡片显示
   - [ ] 显示所有项目卡片（按创建时间倒序）
   - [ ] 每个卡片显示项目名称、状态、创建时间
   - [ ] 已完成项目显示缩略图
   - [ ] 已完成项目显示切片数量和耗时

2. 项目状态实时更新
   - [ ] pending 状态显示 "排队中" + 时钟图标
   - [ ] processing 状态显示 "正在切片" + 旋转动画 + 实时计时
   - [ ] 卡片每 3 秒轮询状态更新
   - [ ] 完成后自动刷新显示缩略图
   - [ ] failed 状态显示 "处理失败" + 错误图标

3. 项目卡片交互
   - [ ] 点击项目卡片跳转到项目详情页
   - [ ] 悬停时卡片阴影加深
   - [ ] 处理中的卡片有高亮边框

4. 空状态
   - [ ] 无项目时显示空状态提示

---

### 模块 5：项目详情页 (Project Detail)
**测试场景：**
1. 页面加载与布局
   - [ ] 页面成功加载，显示项目名称
   - [ ] 显示返回按钮
   - [ ] 显示项目状态、切片数量、耗时
   - [ ] 显示标签筛选栏

2. 返回导航
   - [ ] 点击返回按钮回到首页
   - [ ] 浏览器后退按钮正常工作

3. 处理中状态
   - [ ] processing 状态显示 "正在处理中..." 提示
   - [ ] 无切片时不显示切片网格

4. 失败状态
   - [ ] failed 状态显示错误信息
   - [ ] 显示错误详情（如果有）

---

### 模块 6：切片网格 - 显示与筛选 (Clip Grid)
**测试场景：**
1. 切片卡片显示
   - [ ] 显示所有切片卡片（网格布局）
   - [ ] 每个卡片显示缩略图、时间范围、话题
   - [ ] 显示切片索引号
   - [ ] 显示时长角标
   - [ ] 显示标签（最多 6 个）
   - [ ] 超过 6 个标签显示 "+N"

2. 标签筛选功能
   - [ ] 显示所有标签维度和值
   - [ ] 点击标签激活筛选（标签高亮）
   - [ ] 筛选后切片列表更新
   - [ ] 显示筛选结果数量
   - [ ] 点击 "清除" 按钮重置筛选
   - [ ] 同一维度可选择多个值（OR 逻辑）

3. 响应式布局
   - [ ] 桌面端显示 4 列
   - [ ] 平板端显示 3 列
   - [ ] 移动端显示 1 列

---

### 模块 7：切片播放与下载 (Clip Playback)
**测试场景：**
1. 视频播放
   - [ ] 点击缩略图上的播放按钮播放视频
   - [ ] 视频自动播放
   - [ ] 显示视频控制条
   - [ ] 视频播放结束后自动切回缩略图
   - [ ] 点击卡片底部 "播放" 按钮播放视频

2. 悬停预览
   - [ ] 悬停在缩略图上显示播放按钮
   - [ ] 播放按钮有透明背景

3. 下载功能
   - [ ] 点击下载按钮触发视频下载
   - [ ] 下载的文件名正确

---

### 模块 8：全局片段库 (Global Clip Search)
**测试场景：**
1. 标签筛选面板
   - [ ] 显示所有标签维度（按维度分组）
   - [ ] 每个维度显示最多 15 个值
   - [ ] 超过 15 个值显示 "+N"
   - [ ] 点击标签激活筛选（标签高亮）
   - [ ] 自动触发搜索并显示结果
   - [ ] 点击已激活标签取消筛选
   - [ ] 可同时选择多个维度的标签

2. 关键词搜索
   - [ ] 输入关键词后点击 "搜索" 按钮
   - [ ] 按 Enter 键触发搜索
   - [ ] 搜索转录文本、话题、摘要
   - [ ] 显示搜索结果数量
   - [ ] 搜索中显示 "搜索中..." 状态

3. 组合筛选
   - [ ] 标签 + 关键词组合筛选
   - [ ] 点击 "X" 按钮清除所有筛选

4. 搜索结果显示
   - [ ] 显示匹配的切片卡片
   - [ ] 每个卡片显示来源项目名称
   - [ ] 点击项目名称跳转到项目详情页
   - [ ] 显示 "找到 N 个切片" 统计
   - [ ] 超过 40 个结果显示 "(显示前 40 个)"

5. 空状态
   - [ ] 未搜索时显示 "选择标签或输入关键词开始搜索"
   - [ ] 无结果时显示 "没有找到匹配的切片"

---

### 模块 9：设置页 - 本地目录监听 (Watch Directory)
**测试场景：**
1. 页面加载
   - [ ] 页面成功加载，显示 "设置" 标题
   - [ ] 显示 "本地目录监听" 说明
   - [ ] 显示添加目录输入框

2. 添加监听目录
   - [ ] 输入有效目录路径
   - [ ] 设置扫描间隔（默认 600 秒）
   - [ ] 点击 "+" 按钮添加成功
   - [ ] 按 Enter 键添加成功
   - [ ] 添加后输入框清空
   - [ ] 目录列表更新

3. 目录列表显示
   - [ ] 显示所有监听目录
   - [ ] 显示目录路径、启用状态、扫描间隔
   - [ ] 显示关联的项目数量
   - [ ] 显示上次扫描时间

4. 启用/禁用目录
   - [ ] 点击 Toggle 按钮切换启用状态
   - [ ] 启用时图标高亮
   - [ ] 禁用时图标灰色
   - [ ] 按 Enter 键切换状态

5. 修改扫描间隔
   - [ ] 下拉选择不同间隔（1分钟、5分钟、10分钟、30分钟、1小时）
   - [ ] 修改后立即保存

6. 删除目录
   - [ ] 点击删除按钮显示确认弹窗
   - [ ] 确认后目录从列表移除
   - [ ] 取消后目录保留

7. 空状态
   - [ ] 无监听目录时显示 "暂未配置监听目录"

8. 错误处理
   - [ ] 添加无效路径显示错误提示
   - [ ] 添加重复路径显示错误提示

---

## 无障碍性测试 (Accessibility)
**测试场景：**
1. 键盘导航
   - [ ] Tab 键可遍历所有交互元素
   - [ ] Enter/Space 键可激活按钮
   - [ ] Esc 键可关闭弹窗（如有）

2. 焦点状态
   - [ ] 所有交互元素有可见焦点环
   - [ ] 焦点顺序符合逻辑

3. ARIA 标签
   - [ ] Icon 按钮有 aria-label
   - [ ] 表单控件有 label 或 aria-label
   - [ ] 图片有 alt 文本

---

## 性能测试 (Performance)
**测试场景：**
1. 页面加载性能
   - [ ] 首页 LCP < 2.5s
   - [ ] 无明显的 CLS（图片有尺寸）

2. 交互响应
   - [ ] 按钮点击响应 < 100ms
   - [ ] Tab 切换流畅无卡顿
   - [ ] 大列表滚动流畅

---

## 跨浏览器测试 (Cross-Browser)
**测试场景：**
1. Chrome/Chromium
   - [ ] 所有功能正常

2. Firefox（可选）
   - [ ] 所有功能正常

3. Safari（可选）
   - [ ] 所有功能正常

---

## 测试数据准备

### 前置条件
1. 数据库已初始化（Prisma migrate）
2. Redis 服务运行中
3. Engine API 服务运行中 (localhost:8080)
4. Next.js 服务运行中 (localhost:3001)
5. BullMQ Worker 运行中

### 测试数据
1. 准备 1 个有效的测试视频文件（< 500MB，MP4 格式）
2. 准备 1 个无效文件（.txt）
3. 准备 1 个有效的本地视频路径
4. 准备 1 个有效的本地目录路径

---

## 测试执行顺序

### 阶段 1：基础功能（无需数据）
1. 首页导航与布局
2. Tab 切换
3. 设置页导航

### 阶段 2：上传与项目创建
1. 文件上传（错误场景）
2. 文件上传（成功场景）
3. 本地路径提交
4. 项目列表显示

### 阶段 3：项目详情与切片（需等待处理完成）
1. 项目状态轮询
2. 项目详情页加载
3. 切片网格显示
4. 标签筛选
5. 视频播放与下载

### 阶段 4：全局搜索
1. 标签筛选
2. 关键词搜索
3. 组合搜索

### 阶段 5：设置功能
1. 添加监听目录
2. 启用/禁用目录
3. 修改间隔
4. 删除目录

---

## 预期测试时长
- 自动化测试执行：约 10-15 分钟
- 问题修复：根据发现的问题数量
- 回归测试：约 5-10 分钟

---

## 成功标准
- 所有测试场景通过率 ≥ 95%
- 无严重阻塞性 Bug
- 无控制台错误（除预期的错误提示）
- 所有交互响应流畅
